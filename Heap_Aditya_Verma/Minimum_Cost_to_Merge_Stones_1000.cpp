#include<iostream>
#include<queue>
#include<vector>
using namespace std;

int main()
{
    return 0;
}

int mergeStones(vector<int>& stones, int k) {
    int result = 0;
    int n = stones.size();
    priority_queue< pair<int, vector<int> >, vector< pair<int, vector<int>> >, greater< pair<int, vector<int>> >> mini;

    for (int i = 0; i < n - k + 1; i++)
    {

    }

    return 0;
}